{% extends 'base.html.twig' %}

{% block title %}Geooption{% endblock %}

{% block body %}
    <h1>Geooption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ geooption.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ geooption.Name }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ geooption.Text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('geooption_index') }}">back to list</a>

    <a href="{{ path('geooption_edit', {'id': geooption.id}) }}">edit</a>

    {{ include('geooption/_delete_form.html.twig') }}
{% endblock %}