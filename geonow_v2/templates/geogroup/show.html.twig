{% extends 'base.html.twig' %}

{% block title %}Geogroup{% endblock %}

{% block body %}
    <h1>Geogroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ geogroup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ geogroup.name }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ geogroup.text }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ geogroup.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('geogroup_index') }}">back to list</a>

    <a href="{{ path('geogroup_edit', {'id': geogroup.id}) }}">edit</a>

    {{ include('geogroup/_delete_form.html.twig') }}
{% endblock %}
